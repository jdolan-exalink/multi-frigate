# Environment variables for Docker Compose deployment

# Backend configuration
JWT_SECRET=your-super-secret-jwt-key-here

# Frontend configuration
FRIGATE_PROXY=http://backend:4000
OPENID_SERVER=https://your.keycloak.server.com:443/realms/your-realm
REALM=your-realm-name
CLIENT_ID=your-frontend-client-id

# Optional: Database path (defaults to ./DB/users.db)
# DB_PATH=./DB/users.db

# Optional: Backend port (defaults to 4000)
# PORT=4000